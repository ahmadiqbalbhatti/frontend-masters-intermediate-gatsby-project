{
  "private": true,
  "workspaces": [
    "site",
    "gatsby-theme-shared-nav"
  ],
  "scripts": {
    "develop": "npm run develop --workspace=site",
    "build": "npm run build --workspace=site"
  }
}
